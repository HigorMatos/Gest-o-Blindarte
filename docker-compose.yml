version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: gesto
      POSTGRES_PASSWORD: gesto
      POSTGRES_DB: gesto
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
